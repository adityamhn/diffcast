# GitHub
GITHUB_TOKEN=ghp_xxx          # For API calls (fetching diffs). Create at: https://github.com/settings/tokens
# Webhook secret: use ONE of these:
# - GITHUB_WEBHOOK_SECRET=xxx     # Shared secret for all repos (set same in each repo's webhook)
# - Or register each repo via POST /api/repos/register to get per-repo secrets

# Firebase
FIREBASE_SERVICE_ACCOUNT_PATH=path/to/serviceAccountKey.json
# Or use: GOOGLE_APPLICATION_CREDENTIALS=path/to/serviceAccountKey.json
FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com

# LLM + Pipeline
GEMINI_API_KEY=your-gemini-key
PIPELINE_LANGUAGES=en,es,fr
PIPELINE_MAX_WORKERS=2
PIPELINE_TARGET_DURATION_SEC=26
PIPELINE_ENABLE_VEO=true
PIPELINE_VEO_MODEL_DEFAULT=veo-3.1-generate-preview
PIPELINE_VEO_MODEL_FAST=veo-3.1-fast-generate-preview
PIPELINE_VEO_TIMEOUT_SEC=180
PIPELINE_VEO_USE_REFERENCE_IMAGES=true   # Set false if in EU/UK/CH/MENA or hitting "use case not supported" errors
PIPELINE_TTS_MODEL=gemini-2.5-flash-preview-tts
PIPELINE_TTS_MODEL_FALLBACK=gemini-2.5-pro-preview-tts
PIPELINE_ENABLE_STYLE_IMAGES=false
PIPELINE_STYLE_IMAGE_MODEL=gemini-3-pro-image-preview
PIPELINE_ALLOWED_LOCAL_VIDEO_ROOTS=backend/tests/videos
# Optional JSON map to control voice names per language code
# PIPELINE_VOICE_MAP={"en":"en-US-Neural2-C","es":"es-ES-Neural2-B"}

# Flask
FLASK_ENV=development
SECRET_KEY=your-secret-key
LOG_LEVEL=INFO
